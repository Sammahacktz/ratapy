"""Reusable curses toolkit for the RATA control panel.

Deliberately small and free of business logic: a screen/colour bootstrap
(``screen``) and a handful of drawing widgets (``widgets``) -- boxes with
titled borders, a keyboard/mouse-driven menu, and a scrolling log pane. The
application (``ratapyUI.app``) composes these into pages.
"""
